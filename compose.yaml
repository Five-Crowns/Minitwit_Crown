version: '3.8'  # Specify the Docker Compose version

services:
  server:
    build:
      context: .  # Build from the current directory
      target: final  # Optional: Use if your Dockerfile has multiple stages
    ports:
      - "8080:8080"  # Map host port 8080 to container port 8080
    volumes:
      - ./src/Chirp.Infrastructure/Data:/app/Data:Z

# Example PostgreSQL service (uncomment if needed)
#   db:
#     image: postgres
#     restart: always
#     user: postgres
#     secrets:
#       - db-password
#     volumes:
#       - db-data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=example
#       - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
#     expose:
#       - 5432
#     healthcheck:
#       test: [ "CMD", "pg_isready"