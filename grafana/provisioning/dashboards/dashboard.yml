apiVersion: 1

#This loads dashboards from files instead of manually creating them each time
providers:
  - name: "default"
    folder: ""
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards